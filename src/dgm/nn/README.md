# nn -- Shared Neural Network Building Blocks

Reusable layers and modules used across model families: attention mechanisms, MLPs, positional encodings, and normalization layers.

## Status

Components will be added as model implementations require them. Shared building blocks prevent duplication across `dgm.vae`, `dgm.diffusion`, `dgm.flow_matching`, etc.
