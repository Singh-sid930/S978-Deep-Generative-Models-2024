# data -- Dataset Utilities

Dataset wrappers, transforms, and data loading helpers for common benchmarks (MNIST, CIFAR-10, CelebA).

## Status

Dataset utilities will be added as model implementations require them. Expect thin wrappers around `torchvision.datasets` with standardized transforms.
