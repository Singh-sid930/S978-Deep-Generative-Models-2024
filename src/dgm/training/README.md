# training -- Training Infrastructure

Shared training loops, callbacks, logging, and evaluation metrics for all model families.

## Status

Training utilities will be added as model implementations require them. Expect standard PyTorch training loops with configurable callbacks and metric tracking.
